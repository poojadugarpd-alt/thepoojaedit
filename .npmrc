# inngest declares an optional peer on @sveltejs/kit, whose transitive
# @sveltejs/vite-plugin-svelte peers vite@8 and conflicts with our vite@7
# (from vitest). The peer is optional and unused here. See docs/decisions.md D-43.
legacy-peer-deps=true
