export const metadata = { title: "Install on iPhone" };

/**
 * STUB — full content (steps, standalone-mode detection, domain-cutover
 * reinstall note, WebKit/Apple doc citations) is Stage 3 of the admin-PWA
 * plan (docs/admin-pwa-plan.md, docs/admin-pwa-setup.md). This placeholder
 * exists only so the "More" menu's link isn't dead during Stage 2.
 */
export default function AdminInstallPage() {
  return (
    <div className="space-y-3">
      <h1 className="text-xl font-semibold">Install on iPhone</h1>
      <p className="text-sm text-ink-soft">
        Install steps are coming in the next part of this project. For now, sign in at
        this address in Safari to use the admin from your phone&rsquo;s browser.
      </p>
    </div>
  );
}
