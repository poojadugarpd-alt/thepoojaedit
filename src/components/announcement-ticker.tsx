/**
 * Top-of-page announcement — an inset rounded sand bar, one calm line, centred.
 * Single statement, no marquee, no dismiss.
 */
export function AnnouncementTicker() {
  return (
    <div className="u-bar">
      <p className="px-4 py-2.5 text-center text-[0.6875rem] font-bold uppercase tracking-[0.09em] text-ink-soft">
        Considered clothing, new and second-life
      </p>
    </div>
  );
}
